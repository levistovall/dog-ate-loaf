cmake_minimum_required(VERSION 2.8.9)
project (math-building-blocks)
set(CMAKE_BUILD_TYPE Release) 
include_directories(include include/other)
#file(GLOB SOURCES "src/*.cpp")
add_subdirectory(src/other)
add_subdirectory(test)
add_executable(isNumberPrime src/is-prime-number.cpp)# ${SOURCES})
target_link_libraries(isNumberPrime PrimeRat)
enable_testing()
add_test(NAME RationalTest COMMAND testRational)
add_test(NAME PrimeAssistantTest COMMAND testPrimeAssistant)
